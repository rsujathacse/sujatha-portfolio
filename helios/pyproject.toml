[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "helios"
version = "1.1.0"
description = "Helios hybrid EDA job fabric CLI and API (slice 1.1)"
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Sujatha R" }]

[project.scripts]
helios = "helios.cli:main"

[tool.setuptools.packages.find]
where = ["."]
include = ["helios*"]

[project.optional-dependencies]
dev = ["pytest>=8"]
